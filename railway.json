{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "mkdir -p /app/writable/cache /app/writable/debugbar /app/writable/logs /app/writable/session && chmod -R 775 /app/writable && chown www-data:www-data /app/writable && php-fpm -D && nginx -g 'daemon off;'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}